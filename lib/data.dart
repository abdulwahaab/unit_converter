import 'package:flutter/material.dart';

class DataConverter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Data Converter'),
      ),
      body: Center(
        child: Text(
          'Data Calculator',
        ),
      ),
    );
  }
}
