import 'package:flutter/material.dart';

class PowerConverter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Power Converter'),
      ),
      body: Center(
        child: Text(
          'Power Calculator',
        ),
      ),
    );
  }
}
