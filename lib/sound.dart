import 'package:flutter/material.dart';

class SoundConverter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sound Converter'),
      ),
      body: Center(
        child: Text(
          'Sound Calculator',
        ),
      ),
    );
  }
}
