import 'package:flutter/material.dart';

BoxDecoration roundElementBox = new BoxDecoration(
  color: Colors.green,
  borderRadius: new BorderRadius.only(
    topLeft: const Radius.circular(10.0),
    topRight: const Radius.circular(10.0),
  ),
);
