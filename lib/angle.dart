import 'package:flutter/material.dart';

class AngleConverter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Angle Converter'),
      ),
      body: Center(
        child: Text(
          'Angle Calculator',
        ),
      ),
    );
  }
}
