import 'package:flutter/material.dart';

class FuelConverter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fuel Converter'),
      ),
      body: Center(
        child: Text(
          'Fuel Calculator',
        ),
      ),
    );
  }
}
