import 'package:flutter/material.dart';

class CompassConverter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Compass Converter'),
      ),
      body: Center(
        child: Text(
          'Compass Calculator',
        ),
      ),
    );
  }
}
